spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:codegate-test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=false;MODE=PostgreSQL

  jpa:
    generate-ddl: true

logging:
  level:
    root: info
